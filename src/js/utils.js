
/**
 * @function importFrom
 * @description import all the modules contained in argument
 * @param {*} r 
 * @returns {Object}
 */

export default function importFrom(r) {
  return r.keys().map(r);
}
